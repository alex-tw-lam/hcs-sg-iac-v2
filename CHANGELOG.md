# Changelog

Semantic versioning; pre-1.0 minors carry breaking changes. Per-release
history lives in the git log and tags (v0.1.0 …) — the still-relevant
breaking change, for the record:

- **v0.2.0**: `--execute` and all interactive prompts were removed;
  `--yes` became the single write gate (dry run is the default).
